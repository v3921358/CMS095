var status = -1;
function enter(pi) {
  // if (pi.getQuestStatus(20730) == 1) {
  pi.openNpc(1063018, "人偶师任务对话");
  // } else {
  //   //pi.warp(910050300,0);
  //   pi.playerMessage(5, "当前无法进入。");

  // }
}
