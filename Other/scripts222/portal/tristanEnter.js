function enter(pi) {
	pi.warp(105100101,0);
	// pi.playerMessage(5,"此传送门不可用.");
	// pi.removeNpc(105100101, 1061015);
	// var map = pi.getMap(105100101);
	// map.spawnNpc(1061015, new java.awt.Point(30, 180));
}